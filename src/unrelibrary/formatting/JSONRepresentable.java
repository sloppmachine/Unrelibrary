package unrelibrary.formatting;

// this interface is meant for objects that are, at some point, meant to be transformed into a json
public interface JSONRepresentable {
    public String toJSON();
}
